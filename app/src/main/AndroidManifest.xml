<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nbs.mykotlinlearning">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".App">
        <activity android:name=".basic.MainActivity" />
        <activity android:name=".basic.TempratureConverterActivity" />
        <activity android:name=".basic.PrimitiveDataReceiverActivity" />
        <activity android:name=".basic.ParcelableReceiverActivity" />
        <activity android:name=".basic.GiveBackActivity" />
        <activity android:name=".viewgroup.MovieActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".viewgroup.DetilMovieActivity" />
        <activity android:name=".sharedpreference.SimplePrefsActivity" />
        <activity android:name=".http.nolibrary.WeatherActivity" />
        <activity android:name=".http.retrofit.view.ForecastActivity"></activity>
    </application>

</manifest>